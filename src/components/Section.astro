---
const { className, title } = Astro.props;
---

<section class:list={["flex flex-col gap-4", className]}>
  {title && <h3 class="relative flex w-full items-center gap-3 pb-4 text-3xl print:gap-1 print:pb-0 print:pt-4 print:text-xl print:font-bold">{title}</h3>}
  <slot />
</section>
